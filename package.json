{
  "name": "gaurav-nursery",
  "version": "2.0.0",
  "private": true,
  "engines": { "node": ">=20.0.0", "npm": ">=10" },
  "description": "Multi-vendor nursery ecommerce platform.",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "concurrently -n customer,seller,admin,api \"npm run dev --workspace @gaurav-nursery/customer-web\" \"npm run dev --workspace @gaurav-nursery/seller-panel\" \"npm run dev --workspace @gaurav-nursery/admin-panel\" \"npm run dev --workspace @gaurav-nursery/backend-api\"",
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "legacy:dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": { "concurrently": "^9.1.2" }
}
