import '../../../seller-panel/app/styles.css';
import '../../../seller-panel/app/seller-extra.css';

export default function SellerLayout({ children }: Readonly<{ children: React.ReactNode }>) {
  return children;
}
